import Room from "@/app/(models)/room";
import RoomForm from "@/app/componants/RoomForm";
import React from "react";

const page = () => {
  return (
    <div>
      <RoomForm />
    </div>
  );
};

export default page;
